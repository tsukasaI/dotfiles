# Claude Code PreToolUse allowlist
# Commands listed here bypass the blocklist even if they match a pattern.
# Format: one command prefix per line. Lines starting with # are comments.
#
# Example: "secretlint" would otherwise be blocked by the *secret* rule.

secretlint
detect-secrets
gitleaks
trufflehog
